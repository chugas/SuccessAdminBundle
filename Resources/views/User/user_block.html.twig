{% block user_block %}

  {{ selector_locales_render() }}

  {% if app.user %}
    {{ ' | ' ~ app.user.username }}

    <a href="{{ url('usuario_admin_logout') }}">{{ 'user_block_logout'|trans({}, 'SonataUserBundle') }}</a>
  {% endif %}

{% endblock %}
